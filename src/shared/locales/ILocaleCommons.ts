export default interface ILocaleCommons {
  common: {
    save: string;
    cancel: string;
  };
  validation: {
    generic: string;
  };
  glossary: {
    home: string;
    transaction: string;
    category: string;
    statistics: string;
    options: string;
  };
}
